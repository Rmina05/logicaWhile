var numeroentrada =0;
var resultado =0;
var control =0
numeroentrada = parseInt(prompt("ingrese el numero que desea calcular"));
while (numeroentrada>=control) {
    resultado = resultado + control;
    control = control+1;
}
alert("la suma de los primeros " + numeroentrada + " natural es igual a " + resultado);